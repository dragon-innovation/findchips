source "https://rubygems.org"

# Specify your gem's dependencies in findchips.gemspec
gemspec

group :test do
  gem 'rspec_junit_formatter'           # JUnit formatter for Circle CI output
  gem 'webmock'
end
